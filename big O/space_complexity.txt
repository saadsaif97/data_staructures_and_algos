-- Most primitives (booleans, numbers, undefined and null) are constant space
-- String require O(n) space (where n is the length of the string)
-- Reference types (Arrays and Objects) require O(n) space (where n is length of array or number of keys of object) 